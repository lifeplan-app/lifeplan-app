# Phase 2.5 Critical 10件 修正 実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Phase 2 で検出された 10 件の Critical バグを `index.html` で修正し、Phase 1 スナップショットを意図した方向に更新してコミット。Phase 3（計算ロジック分離）へ安全に移行できる状態にする。

**Architecture:** 6 グループに分けて順次修正。各グループで「期待方向の事前宣言 → コード修正 → `npm test` で失敗確認 → `git diff` で方向性目視 → `test:update` で snapshot 承認 → 1 コミット」のサイクルを回す。Phase 1 スナップショットが安全網として機能する。

**Tech Stack:** Vanilla JS（index.html 内）、Vitest 2.x、Playwright 1.x（既存）

**基準設計書:** `docs/superpowers/specs/2026-04-24-phase2.5-critical-fixes-design.md`

**リポジトリ:** `/Users/nagatohiroki/ライフプランアプリ/`（main ブランチ直接作業）

**⚠️ 重要な前提:**
- 日本語パス `/Users/nagatohiroki/ライフプランアプリ/` はシェルで必ず **ダブルクォート** で囲む
- `node` / `npm` は nvm 経由（`source ~/.nvm/nvm.sh && nvm use default >/dev/null 2>&1 &&` を各コマンドに付ける）
- **これは Phase 1/2 と違い `index.html` を編集するフェーズ**。修正ごとにスナップショット再生成が必要
- `DESIGN_GUIDELINES.md`（White Pearl Cosmos / 2フォント体系）に UI 追加は準拠
- 各グループ完了後に `expected-changes.md` へ「期待方向」と「実測 diff サマリー」を追記

---

## File Structure

### 新規作成

| パス | 役割 |
|------|------|
| `docs/phase2-5-fixes/expected-changes.md` | 各 Critical の期待方向と実測を記録（時系列・グループ順に追記） |

### 修正

| パス | 変更箇所 |
|------|----------|
| `index.html` | 6 グループ分の計算・UI 修正。各グループで該当部分のみ変更 |
| `test/__snapshots__/scenario-snapshot.test.js.snap` | 計算変更に応じて意図的に更新（`test:update` 経由） |
| `docs/phase2-audits/02-income-expense.md` 〜 `09-integrated-simulation.md` | 該当 Critical に `[Resolved in Phase 2.5 commit XXXX]` 注記追加（最終タスクで一括） |

### 変更しない

- `test/helpers/playwright-runner.js`、他のテストファイル、`package.json`
- Phase 1/2 成果物（テンプレート、監査レポート本文、設計書、プラン）

---

## 共通の検証ワークフロー（各グループで繰り返す）

各グループで以下の流れを守る：

1. **期待方向を事前宣言** → `docs/phase2-5-fixes/expected-changes.md` に Group N セクションを追加
2. **コード修正** → `index.html` を編集
3. **失敗確認**:
   ```bash
   source ~/.nvm/nvm.sh && nvm use default >/dev/null 2>&1 && cd "/Users/nagatohiroki/ライフプランアプリ" && npm test
   ```
   Expected: Phase 1 snapshot テストが赤くなる（計算が変わったため）。既存ユニットテスト 128 + smoke 2 はグリーンのまま。
4. **差分を目視**:
   ```bash
   cd "/Users/nagatohiroki/ライフプランアプリ" && git diff test/__snapshots__/scenario-snapshot.test.js.snap | head -200
   ```
   宣言した方向と一致、NaN/Infinity/極端値なしを確認。
5. **snapshot 承認**:
   ```bash
   source ~/.nvm/nvm.sh && nvm use default >/dev/null 2>&1 && cd "/Users/nagatohiroki/ライフプランアプリ" && npm run test:update
   ```
6. **全テストグリーン確認**:
   ```bash
   source ~/.nvm/nvm.sh && nvm use default >/dev/null 2>&1 && cd "/Users/nagatohiroki/ライフプランアプリ" && npm test
   ```
   Expected: 155/155 グリーン。
7. **実測サマリーを expected-changes.md に追記**
8. **1 コミット**（`index.html` + snapshot + expected-changes.md をまとめて）

---

## Task 1: セットアップ（expected-changes.md 雛形作成）

**Files:**
- Create: `docs/phase2-5-fixes/expected-changes.md`

- [ ] **Step 1: ディレクトリ作成**

Run:
```bash
mkdir -p "/Users/nagatohiroki/ライフプランアプリ/docs/phase2-5-fixes" && ls "/Users/nagatohiroki/ライフプランアプリ/docs/phase2-5-fixes"
```
Expected: ディレクトリが存在し中身は空。

- [ ] **Step 2: `expected-changes.md` 雛形を作成**

Create `docs/phase2-5-fixes/expected-changes.md` with content:

```markdown
# Phase 2.5 修正の期待方向と実測

各グループ修正の「事前に宣言した期待方向」と「実際のスナップショット差分サマリー」を時系列で記録。

## 使い方

各グループ実施前に「期待方向」を追記 → 修正後に「実測サマリー」を追記。
Phase 2 監査レポートの該当 Critical にここを参照するリンクを張る予定。

---

## Group 1: パートナー年齢バグ修正（02-C01 / 06-C01）

### 期待方向
（Group 1 実施時に記入）

### 実測サマリー
（Group 1 修正後に記入。commit SHA・代表シナリオの `annualIncome` 変化など）

---

## Group 2: 年金関数重複解消（04-C01）

### 期待方向
（Group 2 実施時に記入）

### 実測サマリー
（Group 2 修正後に記入）

---

## Group 3: 住宅ローン控除の構造修正（05-C01 / C02 / C03）

### 期待方向
（Group 3 実施時に記入）

### 実測サマリー
（Group 3 修正後に記入）

---

## Group 4: partnerExpenseChange 経路整合（06-C02）

### 期待方向
（Group 4 実施時に記入）

### 実測サマリー
（Group 4 修正後に記入）

---

## Group 5: プール枯渇の正しい検知（07-C01 / 08-C01）

### 期待方向
（Group 5 実施時に記入）

### 実測サマリー
（Group 5 修正後に記入）

---

## Group 6: インフレ非対称性の解消（09-C01）

### 期待方向
（Group 6 実施時に記入）

### 実測サマリー
（Group 6 修正後に記入）
```

- [ ] **Step 3: コミット**

Run:
```bash
cd "/Users/nagatohiroki/ライフプランアプリ" && git add docs/phase2-5-fixes/expected-changes.md && git commit -m "docs(phase2-5): add expected-changes tracking scaffold"
```

---

## Task 2: Group 1 — パートナー年齢バグ修正（02-C01 / 06-C01）

**Files:**
- Modify: `index.html:17142-17169`（`getIncomeForYearWithGrowth` 内のパートナー収入ブロック）
- Modify: `docs/phase2-5-fixes/expected-changes.md`

**狙い:** `partnerGrowthYears` 計算に「本人の `currentAge`」ではなく「パートナーの年齢」を使う。Task 3 の `02-C01` と Task 7 の `06-C01` は同一バグで、1 行修正で両方解消。

**現状（バグコード、`index.html:17152`）:**
```js
const partnerGrowthYears = Math.max(0, Math.min(yearsElapsed, partnerUntilAge - currentAge));
```

**あるべきコード（`partnerCurrentAge` を追加、fallback 付き）:**
```js
const partnerCurrentAge = partnerBirthYear ? (currentYear - partnerBirthYear) : currentAge;
const partnerGrowthYears = Math.max(0, Math.min(yearsElapsed, partnerUntilAge - partnerCurrentAge));
```

`partnerBirthYear` が null（パートナー生年未設定）の場合は現状と同じ `currentAge` fallback で後方互換性を保つ。

- [ ] **Step 1: 期待方向を `expected-changes.md` に追記**

`docs/phase2-5-fixes/expected-changes.md` の Group 1 セクションに以下を追記：

```markdown
### 期待方向

- **対象**: `getIncomeForYearWithGrowth` (`index.html:17152`) の `partnerGrowthYears` 計算
- **修正内容**: `partnerUntilAge - currentAge` を `partnerUntilAge - partnerCurrentAge` に変更（`partnerCurrentAge = currentYear - partnerBirthYear`、未設定時は `currentAge` fallback）
- **期待される snapshot 差分**:
  - シナリオ B（鈴木健太 35歳 + パートナー年齢差あり）で `annualIncome` が年次的に変化
  - シナリオ A（田中葵・独身）は `partnerBase === 0` なので早期 return、差分なし
  - シナリオ C/D/E も配偶者設定に応じて `annualIncome` が動く
- **確認ポイント**: パートナーが本人より若いシナリオで `annualIncome` が減方向、年上シナリオで増方向
```

- [ ] **Step 2: `index.html:17142-17153` を修正**

Read the block at line 17142-17153 in `index.html` to confirm current content matches expected, then apply Edit to change:

```js
  // ── パートナー収入 ──
  const partnerBirthStr  = state.profile?.partnerBirth || state.profile?.partnerbirth;
  const partnerBirthYear = partnerBirthStr ? new Date(partnerBirthStr).getFullYear() : null;
  const partnerBase      = (parseFloat(state.finance.partnerIncome) || 0) * 12
                          + (parseFloat(state.finance.partnerBonus) || 0);
  const partnerGrowthRate = (parseFloat(state.finance.partnerGrowthRate) || 0) / 100;
  const partnerUntilAge   = parseInt(state.finance.partnerGrowthUntilAge) || untilAge;

  let partnerIncomeThisYear = 0;
  if (partnerBase > 0) {
    const partnerGrowthYears = Math.max(0, Math.min(yearsElapsed, partnerUntilAge - currentAge));
    partnerIncomeThisYear = partnerBase * Math.pow(1 + partnerGrowthRate, partnerGrowthYears);
```

to:

```js
  // ── パートナー収入 ──
  const partnerBirthStr  = state.profile?.partnerBirth || state.profile?.partnerbirth;
  const partnerBirthYear = partnerBirthStr ? new Date(partnerBirthStr).getFullYear() : null;
  const partnerBase      = (parseFloat(state.finance.partnerIncome) || 0) * 12
                          + (parseFloat(state.finance.partnerBonus) || 0);
  const partnerGrowthRate = (parseFloat(state.finance.partnerGrowthRate) || 0) / 100;
  const partnerUntilAge   = parseInt(state.finance.partnerGrowthUntilAge) || untilAge;
  // [Phase 2.5 02-C01/06-C01 fix] パートナー昇給年数はパートナー自身の年齢基準で計算。生年未設定時は本人年齢 fallback で後方互換。
  const partnerCurrentAge = partnerBirthYear ? (currentYear - partnerBirthYear) : currentAge;

  let partnerIncomeThisYear = 0;
  if (partnerBase > 0) {
    const partnerGrowthYears = Math.max(0, Math.min(yearsElapsed, partnerUntilAge - partnerCurrentAge));
    partnerIncomeThisYear = partnerBase * Math.pow(1 + partnerGrowthRate, partnerGrowthYears);
```

- [ ] **Step 3: `npm test` で失敗確認**

Run:
```bash
source ~/.nvm/nvm.sh && nvm use default >/dev/null 2>&1 && cd "/Users/nagatohiroki/ライフプランアプリ" && npm test 2>&1 | tail -20
```
Expected: `scenario-snapshot.test.js` 内の該当シナリオテストが赤くなる（`annualIncome` の値が変わっているため）。既存 Vitest 128 + smoke 2 はグリーンのまま。

- [ ] **Step 4: 差分を目視確認**

Run:
```bash
cd "/Users/nagatohiroki/ライフプランアプリ" && git diff test/__snapshots__/scenario-snapshot.test.js.snap | head -150
```
Expected: 主にシナリオ B/C/D/E の `annualIncome` 行が変化（シナリオ A は `partnerBase === 0` で変化なし）。NaN / Infinity / 極端な負値がないことを確認。

- [ ] **Step 5: snapshot 更新**

Run:
```bash
source ~/.nvm/nvm.sh && nvm use default >/dev/null 2>&1 && cd "/Users/nagatohiroki/ライフプランアプリ" && npm run test:update 2>&1 | tail -10
```
Expected: 全テストグリーン、snapshot ファイル更新。

- [ ] **Step 6: 実測サマリーを `expected-changes.md` に追記**

`Group 1 > 実測サマリー` に以下のような記述を追加（実数値は差分から取得）：

```markdown
### 実測サマリー

- **commit SHA**: （Step 7 後に更新）
- **snapshot 差分行数**: N 行
- **シナリオ別変化**:
  - A 田中葵: 変化なし（期待通り、独身のため）
  - B 鈴木健太: `annualIncome` が初年度 XXX → YYY 万（パートナー年上方向）
  - C 山本誠: 初年度 XXX → YYY 万
  - D 中村博: 初年度 XXX → YYY 万
  - E 林菜緒: 変化なし（期待通り、シングルマザーのため）
- **方向の評価**: 期待通り / 想定外（以下補足...）
```

- [ ] **Step 7: 全テストグリーン再確認 + コミット**

Run:
```bash
source ~/.nvm/nvm.sh && nvm use default >/dev/null 2>&1 && cd "/Users/nagatohiroki/ライフプランアプリ" && npm test 2>&1 | tail -5
```
Expected: 155 passed.

Run:
```bash
cd "/Users/nagatohiroki/ライフプランアプリ" && git add index.html test/__snapshots__/scenario-snapshot.test.js.snap docs/phase2-5-fixes/expected-changes.md && git commit -m "fix(calc): use partner age for partner income growth duration (02-C01/06-C01)"
```

- [ ] **Step 8: `expected-changes.md` の commit SHA を補完**

コミット後、`expected-changes.md` の Group 1 実測サマリーの `commit SHA` 欄を実 SHA に置換。Edit で反映。

Run:
```bash
cd "/Users/nagatohiroki/ライフプランアプリ" && git log -1 --format=%h
```
でSHAを取得し、Edit で `expected-changes.md` 内の `（Step 7 後に更新）` を置換、追加コミット：

```bash
cd "/Users/nagatohiroki/ライフプランアプリ" && git add docs/phase2-5-fixes/expected-changes.md && git commit -m "docs(phase2-5): record Group 1 actual SHA"
```

---

## Task 3: Group 2 — 年金関数重複解消（04-C01）

**Files:**
- Modify: `index.html` 複数箇所（old `calcPensionEstimate()` とその呼び出し元のリネーム）
- Modify: `docs/phase2-5-fixes/expected-changes.md`

**狙い:** JS 関数宣言の hoisting により `calcPensionEstimate(who)` (`index.html:15129`) が `calcPensionEstimate()` (`index.html:5944`) を覆い、古い呼び出し元（`calcSimpleSim` / `renderTaxSimulation` / UI oninput）で `undefined` が返って NaN 伝播するバグ。古い関数をリネームし、呼び出し元を追従させる。

**古い関数の呼び出し箇所（要リネーム対象）:**
- `index.html:3321` — `<select id="penEmployType" onchange="calcPensionEstimate()">`
- `index.html:3329` — `oninput="calcPensionEstimate()"`
- `index.html:3335` — `oninput="calcPensionEstimate();document.getElementById('penAvgIncomeWarn').style.display='none'"`
- `index.html:3344` — `oninput="calcPensionEstimate()"`
- `index.html:5944` — `function calcPensionEstimate() {`（定義）
- `index.html:5979` — `const pension = calcPensionEstimate(); // 万円/月`
- `index.html:20374` — `const pension = calcPensionEstimate();`
- `index.html:20491` — `const pension = calcPensionEstimate();`

**新しい関数はそのまま保持:**
- `index.html:15129` — `function calcPensionEstimate(who) { ... }` は触らない

- [ ] **Step 1: 期待方向を追記**

`expected-changes.md` の Group 2 セクションに以下を追記：

```markdown
### 期待方向

- **対象**: 古い `calcPensionEstimate()`（引数なし）を `calcSimplePensionEstimate()` にリネーム。呼び出し元 7 箇所（UI onchange/oninput × 4、`calcSimpleSim` × 1、`renderTaxSimulation` × 2）も追従
- **期待される snapshot 差分**: **差分なし**の想定。メインの `calcIntegratedSim` / `calcRetirementSimWithOpts` は `calcPensionEstimate(who)`（新しい方）を経由しないで `retPensionMonthly` を直接読むため、今回のリネームで統合シミュの出力は変わらない
- **確認ポイント**: snapshot が全く動かないこと（他の影響なし）。`calcSimpleSim` と `renderTaxSimulation` の挙動は UI テストがないので目視確認は不要（別タスク扱い）
```

- [ ] **Step 2: `index.html:5944` の関数定義をリネーム**

Edit: `function calcPensionEstimate() {` → `function calcSimplePensionEstimate() {`

- [ ] **Step 3: UI onchange/oninput の 4 箇所を更新**

Edit 各箇所: `calcPensionEstimate()` → `calcSimplePensionEstimate()`
- `index.html:3321` の `onchange="..."`
- `index.html:3329` の `oninput="..."`
- `index.html:3335` の `oninput="..."`（セミコロンの前の部分）
- `index.html:3344` の `oninput="..."`

- [ ] **Step 4: `calcSimpleSim` (L5979) と `renderTaxSimulation` (L20374, L20491) を更新**

Edit 各箇所: `calcPensionEstimate()` → `calcSimplePensionEstimate()`

**注意**: grep で `calcPensionEstimate(` を再確認し、**引数なし**の呼び出しだけを置換する。`calcPensionEstimate('partner')` のような引数ありの呼び出しは触らない。

Run:
```bash
grep -n "calcPensionEstimate(" "/Users/nagatohiroki/ライフプランアプリ/index.html" | head -30
```
Expected: `calcSimplePensionEstimate()` の呼び出しは 7 箇所、`calcPensionEstimate(who)` 呼び出しは引数ありで残っている。

- [ ] **Step 5: `npm test` で失敗なし確認**

Run:
```bash
source ~/.nvm/nvm.sh && nvm use default >/dev/null 2>&1 && cd "/Users/nagatohiroki/ライフプランアプリ" && npm test 2>&1 | tail -10
```
Expected: **155/155 グリーン**（snapshot 変化なしの想定）。もし snapshot が赤くなったら、リネーム漏れ等の副作用が発生している可能性。grep で漏れがないか再確認。

- [ ] **Step 6: 実測サマリー記入 + コミット**

`expected-changes.md` に実測サマリーを追記（差分なしの確認）。

Run:
```bash
cd "/Users/nagatohiroki/ライフプランアプリ" && git add index.html docs/phase2-5-fixes/expected-changes.md && git commit -m "fix(calc): rename legacy calcPensionEstimate to calcSimplePensionEstimate (04-C01)"
```

---

## Task 4: Group 3 — 住宅ローン控除の構造修正（05-C01 / C02 / C03）

**Files:**
- Modify: `index.html` — UI に住宅種別セレクタ追加、`calcMortgageDeduction` 書き換え、`calcIncomeTaxAmount` 新設、`calcIntegratedSim` のローン残高計算置き換え
- Modify: `docs/phase2-5-fixes/expected-changes.md`

**狙い:** 住宅ローン控除の構造的 Critical 3 件を一括修正。UI 変更を伴う最も重い Task。

### 3-A. 住宅種別セレクタ追加（05-C01）

新規 UI 要素として住宅種別ドロップダウンを追加。

**候補の住宅種別と控除条件（2026 年 4 月現在・国税庁 No.1211-1 準拠）:**

| value | 表示名 | 控除率 | 控除期間 | 借入限度額（新築） |
|---|---|---|---|---|
| `general` | 一般住宅 | 0.7% | 10 年 | 2,000 万円（新築は 0 の年もあるが保守的に 2000 を上限とする） |
| `long_term` | 認定長期優良住宅 | 0.7% | 13 年 | 5,000 万円 |
| `low_carbon` | 認定低炭素住宅 | 0.7% | 13 年 | 5,000 万円 |
| `zeh` | ZEH水準省エネ住宅 | 0.7% | 13 年 | 4,500 万円 |
| `energy_saving` | 省エネ基準適合住宅 | 0.7% | 13 年 | 4,000 万円 |

`state.lifeEvents.mortgage.housingType` に保存（既存 JSON 互換性のためデフォルト `general`）。

### 3-B. `calcIncomeTaxAmount` 新設（05-C02）

課税所得から所得税額を概算する簡易関数（国税庁 No.2260 速算表ベース）。

```js
// [Phase 2.5 05-C02 helper] 簡易所得税計算（万円単位）。正確な給与所得控除・各種控除は考慮せず、課税対象所得ベースの概算。
function calcIncomeTaxAmount(taxableIncomeMan) {
  if (taxableIncomeMan <= 0) return 0;
  // 国税庁 No.2260（2026年4月現在）速算表
  const brackets = [
    { upper: 195,  rate: 0.05,  deduction: 0 },
    { upper: 330,  rate: 0.10,  deduction: 97500 / 10000 },
    { upper: 695,  rate: 0.20,  deduction: 427500 / 10000 },
    { upper: 900,  rate: 0.23,  deduction: 636000 / 10000 },
    { upper: 1800, rate: 0.33,  deduction: 1536000 / 10000 },
    { upper: 4000, rate: 0.40,  deduction: 2796000 / 10000 },
    { upper: Infinity, rate: 0.45, deduction: 4796000 / 10000 },
  ];
  for (const b of brackets) {
    if (taxableIncomeMan <= b.upper) {
      return Math.max(0, taxableIncomeMan * b.rate - b.deduction);
    }
  }
  return 0;
}
```

### 3-C. `calcMortgageDeduction` 書き換え（05-C01 + 05-C02）

住宅種別の借入限度を適用し、所得税・住民税で控除額を capping。

```js
// ⑧ [Phase 2.5 05-C01/05-C02 fix] 住宅ローン控除: 住宅種別の借入限度・税額 cap を考慮（万円/年）
function calcMortgageDeduction(year, balance) {
  const p = getRetirementParams();
  const m = state.lifeEvents?.mortgage || {};
  if (!p.mortgageDeductStart || !p.mortgageDeductYears || !balance) return 0;
  const endYear = p.mortgageDeductStart + p.mortgageDeductYears - 1;
  if (year < p.mortgageDeductStart || year > endYear) return 0;

  // 住宅種別ごとの借入限度額（万円）
  const HOUSING_TYPES = {
    general:       { limit: 2000 },
    long_term:     { limit: 5000 },
    low_carbon:    { limit: 5000 },
    zeh:           { limit: 4500 },
    energy_saving: { limit: 4000 },
  };
  const housingType = m.housingType || 'general';
  const loanLimit = HOUSING_TYPES[housingType]?.limit ?? 2000;
  const controlledBalance = Math.min(balance, loanLimit);

  // 控除額（上限前）= 残高 × 0.7%
  const rawDeduction = controlledBalance * 0.007;

  // 税額 cap: 所得税 + 住民税（住民税控除上限 9.75 万円/年）
  const incomeMan = ((parseFloat(state.finance?.income) || 0) * 12)
                  + (parseFloat(state.finance?.bonus) || 0);
  // 概算: 課税所得 ≒ 年収 × 0.7（給与所得控除・基礎控除・社保控除の超ざっくり）
  const taxableIncome = incomeMan * 0.7;
  const incomeTax = calcIncomeTaxAmount(taxableIncome);
  const residentTaxCap = 9.75; // 万円/年（国税庁 No.1211）
  const deductCap = incomeTax + residentTaxCap;

  return Math.min(rawDeduction, deductCap);
}
```

### 3-D. `calcIntegratedSim` のローン残高を amortization 化（05-C03）

`index.html:14276-14284` 付近の線形近似を、退職シミュと同じ `calcMortgageSchedule` 結果に置き換え。

現状（例）:
```js
const mortgageBalanceInteg = principal * Math.max(0, 1 - elapsed / term);
```

置換後（退職シミュと同じ amortization を共有）:
```js
// [Phase 2.5 05-C03 fix] 統合シミュも退職シミュと同じ amortization を使う
const schedule = calcMortgageSchedule();
const scheduleRow = schedule.find(r => r.year === yr);
const mortgageBalanceInteg = scheduleRow ? scheduleRow.balance : 0;
```

**注意**: `calcMortgageSchedule()` が返すオブジェクトの正確なキー名は `index.html:12557` 付近を読んで確認。`.balance` か `.principalEnd` かで変える。

### 3-E. UI にセレクタ追加

`index.html` 内の住宅ローンパネル（grep で `mortgagePanel` や `住宅ローン` 関連の UI ブロックを特定）に以下を追加:

```html
<label style="display:block;margin:8px 0 4px">住宅種別（住宅ローン控除）</label>
<select id="leMortgageHousingType" onchange="saveMortgageSettings()">
  <option value="general">一般住宅（控除 10 年・上限 2,000 万）</option>
  <option value="long_term">認定長期優良住宅（13 年・5,000 万）</option>
  <option value="low_carbon">認定低炭素住宅（13 年・5,000 万）</option>
  <option value="zeh">ZEH 水準省エネ住宅（13 年・4,500 万）</option>
  <option value="energy_saving">省エネ基準適合住宅（13 年・4,000 万）</option>
</select>
```

`saveMortgageSettings()` 相当の save 関数で `state.lifeEvents.mortgage.housingType = document.getElementById('leMortgageHousingType').value` を反映。load 関数でセレクタ値を復元（未設定時は `'general'` デフォルト）。

`DESIGN_GUIDELINES.md` を必ず確認し、既存フォーム要素のスタイルと統一すること。

### Task 4 手順

- [ ] **Step 1: 期待方向を `expected-changes.md` に追記**（Group 3 セクション）

「住宅種別セレクタ追加により既存サンプルは `general` 扱い。所得税 cap により低所得シナリオ（E: シングルマザー）で控除額が縮小。amortization 置き換えで中期のローン残高が増える（線形近似より実態が多い）。最終シナリオ B の退職前資産が数十万円〜数百万円減少方向」。

- [ ] **Step 2: `calcIncomeTaxAmount` 関数を新設**

`index.html` の `calcMortgageDeduction` の直前（`index.html:17222` 付近）に `calcIncomeTaxAmount` を挿入。

- [ ] **Step 3: `calcMortgageDeduction` を書き換え**

`index.html:17223-17231` を上記 3-C の内容に置き換え。

- [ ] **Step 4: `calcIntegratedSim` のローン残高計算を修正**

`index.html:14276` 付近の線形近似を上記 3-D で置き換え。

まず `calcMortgageSchedule()` の戻り値構造を確認:
```bash
grep -n "function calcMortgageSchedule" "/Users/nagatohiroki/ライフプランアプリ/index.html"
```
戻り値の行（`return schedule` / `return { ... }` / `.balance` / `.principalEnd` 等）を読んで正確なキー名を決める。

- [ ] **Step 5: UI セレクタ追加**

住宅ローンパネル（`index.html:2860-2890` 付近。grep で `mortgagePanel` や `leMortgage` 関連 UI を特定）に上記 3-E の `<select>` を追加。

対応する save/load の関数（`index.html:14813-14820`（save）と `index.html:14660-14662`（load）付近）にも `housingType` の処理を追加:

Save（該当関数内）:
```js
housingType: document.getElementById('leMortgageHousingType')?.value || 'general',
```

Load（該当関数内）:
```js
document.getElementById('leMortgageHousingType').value = m.housingType || 'general';
```

- [ ] **Step 6: `npm test` で失敗確認**

```bash
source ~/.nvm/nvm.sh && nvm use default >/dev/null 2>&1 && cd "/Users/nagatohiroki/ライフプランアプリ" && npm test 2>&1 | tail -15
```
Expected: snapshot 赤く、`mortgageDeduct` フィールド・ローン残高関連のフィールドが変化。

- [ ] **Step 7: 差分目視 → snapshot 更新 → 実測サマリー追記 → コミット**

```bash
cd "/Users/nagatohiroki/ライフプランアプリ" && git diff test/__snapshots__/scenario-snapshot.test.js.snap | head -200
```

目視で方向確認（ローン残高は増、低所得シナリオで控除額は減）。

```bash
source ~/.nvm/nvm.sh && nvm use default >/dev/null 2>&1 && cd "/Users/nagatohiroki/ライフプランアプリ" && npm run test:update && npm test 2>&1 | tail -5
```
Expected: 155/155 グリーン。

`expected-changes.md` Group 3 実測を追記。

```bash
cd "/Users/nagatohiroki/ライフプランアプリ" && git add index.html test/__snapshots__/scenario-snapshot.test.js.snap docs/phase2-5-fixes/expected-changes.md && git commit -m "fix(mortgage): fix housing type, tax liability, and amortization (05-C01/C02/C03)"
```

- [ ] **Step 8: コミット SHA を expected-changes.md に反映**（Task 2 Step 8 と同じ手順）

---

## Task 5: Group 4 — partnerExpenseChange 経路整合（06-C02）

**Files:**
- Modify: `index.html` — `calcIntegratedSim` と `calcMonteCarlo` に `partnerExpenseChange` 処理を追加
- Modify: `docs/phase2-5-fixes/expected-changes.md`

**狙い:** `state.retirement.partnerExpenseChange`（パートナー退職後の月額支出変化）が `calcRetirementSimWithOpts` と `calcMultiScenario` には反映されているが、`calcIntegratedSim` と `calcMonteCarlo` では完全に欠落している経路間不整合を解消。

**参考コード（反映済みの経路）:**
- `index.html:17604-17605`（`calcRetirementSimWithOpts`）
- `index.html:18400`（`calcMultiScenario`）

上記で使われているロジックを `calcIntegratedSim` と `calcMonteCarlo` の年次ループに移植する。

- [ ] **Step 1: 期待方向追記**

`expected-changes.md` Group 4 に以下を記述：

```markdown
### 期待方向

- **対象**: `calcIntegratedSim`（`index.html:14225-14340`）と `calcMonteCarlo`（`index.html:18580-18646`）に `partnerExpenseChange` の反映を追加
- **期待される差分**: パートナー退職（`partnerTargetAge` 到達）後の年度で `annualExpense` が `partnerExpenseChange * 12` 分だけ変化
- **該当シナリオ**: シナリオ B が `partnerExpenseChange` を設定していれば変化。未設定なら差分ゼロ
- **確認ポイント**: サンプルシナリオに `partnerExpenseChange` が設定されているか事前確認し、ゼロ設定のシナリオで差分が出ないことを確認
```

- [ ] **Step 2: 参考コードを読む**

`index.html:17579-17610` と `index.html:18380-18405` を読んで、`partnerExpenseChange` の適用方法（追加か乗算か、年判定ロジック）を把握。

- [ ] **Step 3: `calcIntegratedSim` の支出計算部に処理追加**

`index.html:14261` 付近の `annualExpense = getExpenseForYear(yr)` 後に、以下を追加：

```js
// [Phase 2.5 06-C02 fix] パートナー退職後の月額支出変化を反映
const r = state.retirement || {};
const partnerTargetAge = parseInt(r.partnerTargetAge);
const partnerBirthStr = state.profile?.partnerBirth || state.profile?.partnerbirth;
const partnerBirthYear = partnerBirthStr ? new Date(partnerBirthStr).getFullYear() : null;
const partnerRetireYear = (partnerBirthYear && partnerTargetAge) ? partnerBirthYear + partnerTargetAge : null;
if (partnerRetireYear && yr >= partnerRetireYear) {
  annualExpense += (parseFloat(r.partnerExpenseChange) || 0) * 12;
}
```

正確な変数名・挿入位置は、参考コード（L17604）での使われ方と合わせること。`r`（retirement state）の参照変数名がループ外で既にあるなら再宣言しない。

- [ ] **Step 4: `calcMonteCarlo` にも同様に追加**

`index.html:18580-18646` の `calcMonteCarlo` 内の支出計算ブロックに、Step 3 と同じロジックを挿入。

- [ ] **Step 5 以降**: npm test → diff 目視 → test:update → expected-changes 追記 → commit

```bash
cd "/Users/nagatohiroki/ライフプランアプリ" && git add index.html test/__snapshots__/scenario-snapshot.test.js.snap docs/phase2-5-fixes/expected-changes.md && git commit -m "fix(calc): apply partnerExpenseChange in integrated sim and Monte Carlo (06-C02)"
```

---

## Task 6: Group 5 — プール枯渇検知修正（07-C01 / 08-C01）

**Files:**
- Modify: `index.html` — `calcIntegratedSim` の二プール枯渇処理（07-C01）と `calcRetirementSimWithOpts` の 4 プール `depleted` 判定（08-C01）
- Modify: `docs/phase2-5-fixes/expected-changes.md`

**狙い:** 投資プール/中間プール枯渇後も計算が続行して架空残高を表示するバグを解消。

### 6-A. 07-C01: 投資プール枯渇後の幻影機会損失停止

`index.html:14225-14340` の `calcIntegratedSim` 内で:
- `_investDeficit *= (1 + _wInvestReturn)` の複利成長を `investPool <= 0` の年以降停止
- `liquidationThisYear` の発火も `investPool > 0` ガード追加
- `investPool = 0` 到達時点で `depleted` フラグ相当を立てる（`simData[y].depleted = true`）

現状（抜粋、`index.html:14302-14318` 付近）:
```js
_investDeficit *= (1 + _wInvestReturn);  // 複利成長
// ...
if (virtualCash < 0) {
  liquidationThisYear = -virtualCash;
  _investDeficit += liquidationThisYear;
  _liquidationEvents.push({ year: yr, amount: liquidationThisYear });
}
// ...
investPool = Math.max(0, investAssetBase - _investDeficit);
```

修正後:
```js
// [Phase 2.5 07-C01 fix] 投資プール枯渇後は機会損失複利と清算を停止
const investPoolHealthy = _investDeficit < investAssetBase;  // 枯渇していない
if (investPoolHealthy) {
  _investDeficit *= (1 + _wInvestReturn);
}
// ...
if (virtualCash < 0 && investPoolHealthy) {
  liquidationThisYear = -virtualCash;
  _investDeficit += liquidationThisYear;
  _liquidationEvents.push({ year: yr, amount: liquidationThisYear });
} else if (virtualCash < 0 && !investPoolHealthy) {
  // 投資プール枯渇中は清算不能 → cashFlow マイナスでそのまま残す
  // （cashPool からも賄えない場合、ユーザー設計上の破綻）
}
// ...
investPool = Math.max(0, investAssetBase - _investDeficit);
const investPoolDepleted = investPool <= 0;
```

判定ロジック（`investPoolHealthy`）を毎年ループ先頭で再計算することで、一度枯渇したら複利を止める。

### 6-B. 08-C01: 4 プール中間枯渇の depleted 判定拡張

`index.html:17700-17779` 付近の `calcRetirementSimWithOpts` 内で、`depleted` 判定を拡張:

現状:
```js
depleted: endAssets <= 0 || isFundingShortfall
```

修正後:
```js
// [Phase 2.5 08-C01 fix] 中間プール枯渇も破綻とみなす：取り崩し順序上必要なプールが 0 になった時点で depleted
const criticalPoolDepleted =
  (indexPool <= 0 && dividendPool <= 0 && cashPool <= 0 && emergencyPool <= 0 && withdrawAmount > 0);
// (withdrawAmount > 0 は「当年取り崩しが必要なのに全プール枯渇」を検知)
depleted: endAssets <= 0 || isFundingShortfall || criticalPoolDepleted
```

変数名は実コードに合わせて調整。取り崩し順序（`cashPool → indexPool → dividendPool → emergencyPool`）で各プールが順に 0 に到達するので、最下位 `emergencyPool` が 0 になった時点で `depleted = true` になる。

### Task 6 手順

- [ ] **Step 1: 期待方向追記**

`expected-changes.md` Group 5 に以下:

```markdown
### 期待方向

- **対象**: `calcIntegratedSim`（二プール）と `calcRetirementSimWithOpts`（4 プール）の枯渇検知強化
- **期待される差分**:
  - FIRE シナリオ（C 山本誠）の出口戦略で、投資プール枯渇年以降の `totalWealth` が「架空プラス」→ 現実に即したマイナス/ゼロ方向
  - `depleted: true` が早い年で立つ
  - 他シナリオ（A/B/D/E）は枯渇しない限り影響なし
- **確認ポイント**: snapshot に `depleted: true` 年が新たに出現するか
```

- [ ] **Step 2: 07-C01 修正**

`calcIntegratedSim` 内で `investPoolHealthy` ガードを追加。

- [ ] **Step 3: 08-C01 修正**

`calcRetirementSimWithOpts` の `depleted` 判定を拡張。

- [ ] **Step 4 以降**: npm test → diff 目視 → test:update → expected-changes 追記 → commit

```bash
cd "/Users/nagatohiroki/ライフプランアプリ" && git add index.html test/__snapshots__/scenario-snapshot.test.js.snap docs/phase2-5-fixes/expected-changes.md && git commit -m "fix(calc): stop phantom compounding after pool depletion (07-C01/08-C01)"
```

---

## Task 7: Group 6 — インフレ非対称性の解消（09-C01）

**Files:**
- Modify: `index.html` — `calcIntegratedSim` の `annualExpense` と `leCost` にインフレ係数を適用
- Modify: `docs/phase2-5-fixes/expected-changes.md`

**狙い:** 現役期の支出・ライフイベント費用にインフレが未適用なのに、退職後シミュでは適用されている非対称性を解消。

**現状**: `index.html:14261` 付近
```js
const annualExpense = getExpenseForYear(yr);
const leCost = calcLECostByYear(yr, opts);
```

**修正後**:
```js
// [Phase 2.5 09-C01 fix] 現役期もインフレ係数を適用（退職シミュとの非対称性解消）
const infRate = (parseFloat(state.finance?.inflationRate) || 2) / 100;
const yearsFromNow = yr - currentYear;
const infFactor = Math.pow(1 + infRate, yearsFromNow);
const annualExpense = getExpenseForYear(yr) * infFactor;
const leCost = calcLECostByYear(yr, opts);
// leCost は内部で複数カテゴリを合算しているので個別に適用するには分解が必要
// 簡略実装: leCost 合計にも同じ infFactor を掛ける
const leCostInflated = {
  childcare: leCost.childcare * infFactor,
  education: leCost.education * infFactor,
  mortgage: leCost.mortgage,  // ローンは名目固定（既に償却表で固定額）
  care: leCost.care * infFactor,
  scholarship: leCost.scholarship,  // 奨学金返済も名目固定
};
// 以降、leCost の代わりに leCostInflated を使う（後続コードを書き換え）
```

**注意**:
- ローン返済と奨学金は名目固定（契約額）なのでインフレ適用しない
- `leCost.mortgage` に家賃上昇も含まれるが、家賃は別系統の `riseRate` が既に適用されている可能性 → 実コード確認して二重適用を避ける
- 後続の `totalLE = leCostInflated.childcare + ...` 書き換えが必要

- [ ] **Step 1: 期待方向追記**

`expected-changes.md` Group 6:

```markdown
### 期待方向

- **対象**: `calcIntegratedSim` の `annualExpense` と `leCost` にインフレ係数を適用
- **期待される差分**: 
  - シナリオ A: 26 歳→90 歳の 64 年シミュ。20 年後の `annualExpense` が `1.02^20 ≈ 1.486` 倍に（約 50% 増）
  - 全シナリオで現役期 `annualExpense` が年々増加
  - 最終年の資産残高は減少方向
- **確認ポイント**: シナリオ A の 5 年目・20 年目・40 年目の `annualExpense` を手計算で検算（`1.02^5 ≈ 1.104`、`1.02^20 ≈ 1.486`、`1.02^40 ≈ 2.208`）
```

- [ ] **Step 2: 実装**

`index.html:14256-14300` 付近（`calcIntegratedSim` の年次ループ先頭）にインフレ処理を挿入。`leCost` のインフレ適用は家賃との二重適用を避けるよう慎重に。

- [ ] **Step 3 以降**: npm test → diff 目視（特にインフレ 1.02^n が効いているか手計算で確認）→ test:update → expected-changes 追記 → commit

```bash
cd "/Users/nagatohiroki/ライフプランアプリ" && git add index.html test/__snapshots__/scenario-snapshot.test.js.snap docs/phase2-5-fixes/expected-changes.md && git commit -m "fix(calc): apply inflation to pre-retirement expenses and life events (09-C01)"
```

---

## Task 8: 最終検証と Phase 2 監査レポートへの注記追加

**Files:**
- Modify: `docs/phase2-audits/02-income-expense.md`, `04-pension.md`, `05-mortgage.md`, `06-partner-retirement.md`, `07-two-pool-model.md`, `08-retirement-withdrawal.md`, `09-integrated-simulation.md` — 該当 Critical に `[Resolved in Phase 2.5 commit XXXX]` 注記を追加
- Modify: `docs/phase2-audits/sanity-walkthrough-シナリオB.md` — Phase 2.5 完了後の総合所感を追記

**狙い:** Phase 2 監査レポートに修正完了の注記を入れ、`sanity-walkthrough-シナリオB.md` を現状に合わせて更新。Phase 3 への引き継ぎを明確化。

- [ ] **Step 1: Phase 2 監査レポートに注記追加**

各 Critical の行に `（**[Resolved in Phase 2.5 commit <SHA>]**）` を追記:

- `02-income-expense.md` の `02-C01` セクション
- `04-pension.md` の `04-C01` セクション
- `05-mortgage.md` の `05-C01` / `05-C02` / `05-C03` セクション
- `06-partner-retirement.md` の `06-C01` / `06-C02` セクション
- `07-two-pool-model.md` の `07-C01` セクション
- `08-retirement-withdrawal.md` の `08-C01` セクション
- `09-integrated-simulation.md` の `09-C01` セクション

SHA は各グループのコミットから取得（`git log --oneline -20`）。

- [ ] **Step 2: サニティウォークスルー更新**

`docs/phase2-audits/sanity-walkthrough-シナリオB.md` の末尾に「Phase 2.5 完了後の再評価」セクションを追加。主要な改善点を列挙：

```markdown
## Phase 2.5 完了後の再評価（2026-04-XX）

Phase 2.5 で全 Critical 10 件が修正された結果、シナリオ B の挙動が以下のように変わった：

- 02-C01 修正: パートナー年収の年次推移がパートナー年齢基準に正しく計算されるようになった
- 09-C01 修正: 現役期の生活費が年 2% で増加するようになり、2046 年の支出が約 1.49 倍
- 05-C03 修正: 住宅ローン残高が amortization で計算され、控除額が実態に即した値に
- （他のCritical 修正による挙動変化を列挙）

### 判定の更新

- 全体判定: ⚠️ 違和感あり → ✅ 妥当（Important 項目の修正は Phase 3 以降）
- 主な改善: 最終資産が現実的な水準に近づいた（Phase 2 時点の 1.5〜2 倍過大が解消）
- 残存懸念: Important 43 件は未対応（特に 02-I01 系インフレ統一は Phase 3 で）
```

- [ ] **Step 3: 最終テスト実行**

```bash
source ~/.nvm/nvm.sh && nvm use default >/dev/null 2>&1 && cd "/Users/nagatohiroki/ライフプランアプリ" && npm test 2>&1 | tail -10
```
Expected: 155/155 グリーン。

- [ ] **Step 4: コミット**

```bash
cd "/Users/nagatohiroki/ライフプランアプリ" && git add docs/phase2-audits/ && git commit -m "docs(phase2): mark Criticals resolved in Phase 2.5 and update walkthrough"
```

- [ ] **Step 5: Phase 2.5 完了サマリーを user に報告**

コミット履歴を確認:
```bash
cd "/Users/nagatohiroki/ライフプランアプリ" && git log --oneline -20
```

Phase 2.5 範囲内のコミットを一覧化し、以下を報告:
- 修正された Critical 件数: 10
- 変更されたファイル: `index.html`, `snap`, `expected-changes.md`, Phase 2 audit files
- 次のステップ候補: Phase 3（計算ロジック分離）の本格開始

---

## 完了条件のまとめ

- [ ] 全 Task 1〜8 が完了
- [ ] `docs/phase2-5-fixes/expected-changes.md` が全 6 グループ埋まっている
- [ ] `npm test` で 155/155 グリーン
- [ ] Phase 2 監査レポートの全 Critical に Resolved 注記あり
- [ ] サニティウォークスルーが Phase 2.5 完了後の評価で更新
- [ ] コミット履歴に 6 グループ + 補助コミットが残っている（目安 10 コミット前後）
