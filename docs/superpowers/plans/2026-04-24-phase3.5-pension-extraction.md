# Phase 3.5 年金コア抽出 実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `index.html` の `_calcPensionCore`（13 行の純粋関数）を `calc/pension.js` に抽出し、Phase 4 `04-I02`（年金繰下げ反映）修正時の作業を局所化する。

**Architecture:** Phase 3 と同じ `<script src>` + `vm.runInContext` 方式。ただし対象は 1 関数・1 ファイルのみの最小規模。Phase 1 スナップショット差分ゼロが成功基準（挙動不変）。

**Tech Stack:** Vanilla JS（classic script）、Vitest 2.x、Playwright 1.x

**基準設計書:** `docs/superpowers/specs/2026-04-24-phase3.5-pension-extraction-design.md`

**リポジトリ:** `/Users/nagatohiroki/ライフプランアプリ/`（main ブランチ）

**⚠️ 重要な前提:**
- 日本語パス `/Users/nagatohiroki/ライフプランアプリ/` はシェルで必ず **ダブルクォート** で囲む
- Node は nvm 経由：`source ~/.nvm/nvm.sh && nvm use default >/dev/null 2>&1 &&` を各コマンドに prefix
- 計算ロジックの内容は一切変えない（snapshot 差分ゼロ）

---

## File Structure

### 新規作成

| パス | 想定行数 | 役割 |
|------|---------|------|
| `calc/pension.js` | 約 20 行（コメント含む） | 公的年金コア計算 |

### 変更

| パス | 変更内容 |
|------|----------|
| `index.html` | L14361-14374 の `_calcPensionCore` を削除、`<script src="calc/pension.js">` を L5347 直後（retirement.js の後）に追加 |
| `docs/phase3-extraction/summary.md` | Phase 3.5 セクション追加 |

### 変更しない

- 既存 `calc/*.js`（8 ファイル）
- テスト一式・snapshot
- `calcPensionEstimate(who)`, `calcSimplePensionEstimate`, `calcAndShowDeferralSim`（DOM glue）

---

## Task 1: `calc/pension.js` 抽出

**Files:**
- Create: `calc/pension.js`
- Modify: `index.html` — `_calcPensionCore` 削除（L14361-14374）、`<script src>` 追加（L5347 直後）
- Modify: `docs/phase3-extraction/summary.md` — Phase 3.5 セクション追加

- [ ] **Step 1: `_calcPensionCore` の本体を Read で取得**

Run:
```bash
grep -nE "^function _calcPensionCore\b" "/Users/nagatohiroki/ライフプランアプリ/index.html"
```
Expected: `14361:function _calcPensionCore(employType, koseiYears, avgIncome, kokuminYears) {`

Read で `index.html` の L14361 から次の `function` 宣言直前までを取得（約 13〜14 行）。関数本体を把握。

- [ ] **Step 2: `calc/pension.js` を新規作成**

Create `calc/pension.js` with:

```javascript
// calc/pension.js
// Phase 3.5: 公的年金コア計算（index.html から抽出）
// 依存: なし（完全な純粋関数）
//
// ブラウザ: <script src="calc/pension.js"></script> で関数がグローバル登録される
// Node テスト: test/helpers/load-calc.js 経由で sandbox にロード
//
// 呼び出し元（index.html に残置）:
// - calcPensionEstimate(who)      — DOM 入力を読んでこの関数を呼び出し、結果を DOM に描画
// - calcSimplePensionEstimate()   — 簡易モード用 UI wrapper

function _calcPensionCore(employType, koseiYears, avgIncome, kokuminYears) {
  // [index.html L14361-14374 の内容を忠実にコピー]
}
```

関数本体は Step 1 の Read 結果をそのままコピー（コメント含む）。

- [ ] **Step 3: `index.html` から `_calcPensionCore` を削除**

Edit で L14361 から関数の終端 `}` までを削除。削除範囲は Read で正確に確認。

- [ ] **Step 4: `<script src>` タグを追加**

L5347 の `<script src="calc/retirement.js"></script>` 直後、L5348 の `<script src="calc/scenarios.js"></script>` の直前に挿入。

Read で L5346-5350 付近の正確な形を確認し、Edit で以下を挿入：

```html
    <script src="calc/pension.js"></script>
```

- [ ] **Step 5: `npm test` で 155/155 グリーン確認**

Run:
```bash
source ~/.nvm/nvm.sh && nvm use default >/dev/null 2>&1 && cd "/Users/nagatohiroki/ライフプランアプリ" && npm test 2>&1 | tail -10
```
Expected: 155/155 グリーン、snapshot 差分ゼロ。

- [ ] **Step 6: snapshot 差分確認**

Run:
```bash
cd "/Users/nagatohiroki/ライフプランアプリ" && git diff test/__snapshots__/scenario-snapshot.test.js.snap
```
Expected: 差分なし。

もし差分が出たら抽出が不完全。関数全体が正しくコピーされているか、`<script src>` のロード順が正しいか確認。

- [ ] **Step 7: `summary.md` に Phase 3.5 セクションを追加**

Edit で `docs/phase3-extraction/summary.md` の末尾に以下を追加：

```markdown

---

## Phase 3.5: calc/pension.js（年金コア抽出・Phase 4 支援目的）

- **抽出関数**: `_calcPensionCore(employType, koseiYears, avgIncome, kokuminYears)`
- **calc/pension.js 行数**: 約 N 行
- **index.html 削減行数**: 約 N 行（21,837 → XXX）
- **残置（index.html に残す）**: `calcPensionEstimate(who)`, `calcSimplePensionEstimate`, `calcAndShowDeferralSim`（すべて DOM glue）
- **テスト結果**: 155/155 グリーン、snapshot 差分 0 行
- **commit SHA**: （Step 8 で追記）
- **Phase 4 での活用**: `04-I02`（年金繰下げ反映）修正時、`_calcPensionCore` に `pensionAge` 引数を追加するか `calc/retirement.js` で `adjustRate(pensionAge)` を掛ける。抽出により修正箇所が明確化
```

実数値は `wc -l` で取得した数字を入れる。

- [ ] **Step 8: コミット**

Run:
```bash
cd "/Users/nagatohiroki/ライフプランアプリ" && git add calc/pension.js index.html docs/phase3-extraction/summary.md && git commit -m "refactor(phase3.5): extract _calcPensionCore to calc/pension.js"
```

- [ ] **Step 9: commit SHA を summary.md に補完 + 再コミット**

Run:
```bash
cd "/Users/nagatohiroki/ライフプランアプリ" && git log -1 --format=%h
```
で SHA 取得、Edit で summary.md の `（Step 8 で追記）` を実 SHA に置換。

Run:
```bash
cd "/Users/nagatohiroki/ライフプランアプリ" && git add docs/phase3-extraction/summary.md && git commit -m "docs(phase3.5): record Phase 3.5 actual SHA"
```

---

## 完了条件

- [ ] `calc/pension.js` が作成されている（`_calcPensionCore` 1 関数・約 20 行）
- [ ] `index.html` から `_calcPensionCore` が消えている（`grep -n "^function _calcPensionCore" index.html` で 0 マッチ）
- [ ] `<script src="calc/pension.js">` が `retirement.js` の直後に追加されている
- [ ] `npm test` で 155/155 グリーン
- [ ] `test/__snapshots__/scenario-snapshot.test.js.snap` の差分ゼロ
- [ ] `docs/phase3-extraction/summary.md` に Phase 3.5 セクションあり
- [ ] コミット 2 件（fix コミット + SHA record コミット）

---

## Phase 4 への引き継ぎ

Phase 3.5 完了後、Phase 4 `04-I02`（年金繰下げ反映）修正時の選択肢：

- **経路 A**: `calc/pension.js` の `_calcPensionCore` に繰下げ率引数を追加
  - signature: `_calcPensionCore(employType, koseiYears, avgIncome, kokuminYears, pensionAge = 65)`
  - 関数内部で `adjustRate(pensionAge)` を適用
- **経路 B**: `calc/retirement.js` の `calcRetirementSimWithOpts` で年金収入に `adjustRate(pensionAge)` を掛ける
- **経路 C**: UI の繰下げ選択時に `state.retirement.pensionMonthly` を直接書き換える

いずれの経路でも修正箇所が局所化されている状態で Phase 4 を始められる。
